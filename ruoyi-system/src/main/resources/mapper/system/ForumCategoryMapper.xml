<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ForumCategoryMapper">

	<resultMap id="ForumCategoryResult" type="com.ruoyi.system.domain.ForumCategory">
		<id property="id" column="id" />
		<result property="name" column="name" />
		<result property="description" column="description" />
		<result property="sortOrder" column="sort_order" />
		<result property="createdAt" column="created_at" />
		<result property="updatedAt" column="updated_at" />
	</resultMap>

	<sql id="Base_Column_List">
		id, name, description, sort_order, created_at, updated_at
	</sql>

	<select id="selectAllCategories" resultMap="ForumCategoryResult">
		SELECT <include refid="Base_Column_List" />
		FROM forum_categories
		ORDER BY sort_order ASC, id ASC
	</select>

	<select id="selectCategoryById" resultMap="ForumCategoryResult">
		SELECT <include refid="Base_Column_List" />
		FROM forum_categories
		WHERE id = #{id}
	</select>

</mapper>

