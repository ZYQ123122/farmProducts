<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ForumAttachmentMapper">

	<resultMap id="ForumAttachmentResult" type="com.ruoyi.system.domain.ForumAttachment">
		<id property="id" column="id" />
		<result property="relatedId" column="related_id" />
		<result property="relatedType" column="related_type" />
		<result property="url" column="url" />
		<result property="type" column="type" />
		<result property="fileName" column="file_name" />
		<result property="fileSize" column="file_size" />
		<result property="createdAt" column="created_at" />
	</resultMap>

	<sql id="Base_Column_List">
		id, related_id, related_type, url, type, file_name, file_size, created_at
	</sql>

	<select id="selectAttachmentList" resultMap="ForumAttachmentResult">
		SELECT <include refid="Base_Column_List" />
		FROM forum_attachments
		WHERE related_id = #{relatedId} AND related_type = #{relatedType}
		ORDER BY created_at ASC
	</select>

	<insert id="insertAttachment" parameterType="com.ruoyi.system.domain.ForumAttachment" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO forum_attachments
		(related_id, related_type, url, type, file_name, file_size, created_at)
		VALUES
		(#{relatedId}, #{relatedType}, #{url}, #{type}, #{fileName}, #{fileSize}, GETDATE())
	</insert>

	<delete id="deleteAttachmentById">
		DELETE FROM forum_attachments WHERE id = #{id}
	</delete>

	<delete id="deleteAttachmentsByRelated">
		DELETE FROM forum_attachments 
		WHERE related_id = #{relatedId} AND related_type = #{relatedType}
	</delete>

</mapper>

