<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.BuyerRequirementMapper">

    <resultMap id="BuyerRequirementResult" type="com.ruoyi.system.domain.BuyerRequirement">
        <id property="id" column="id" />
        <result property="buyerId" column="buyer_id" />
        <result property="productName" column="product_name" />
        <result property="quantity" column="quantity" />
        <result property="specs" column="specs" />
        <result property="status" column="status" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id, buyer_id, product_name, quantity, specs, status, created_at, updated_at
    </sql>

    <select id="selectByBuyerId" resultMap="BuyerRequirementResult">
        SELECT
        <include refid="Base_Column_List" />
        FROM buyer_requirement
        WHERE buyer_id = #{buyerId}
        ORDER BY created_at DESC
    </select>

    <select id="selectAll" resultMap="BuyerRequirementResult">
        SELECT
        <include refid="Base_Column_List" />
        FROM buyer_requirement
        ORDER BY created_at DESC
    </select>

    <select id="selectByIdAndBuyerId" resultMap="BuyerRequirementResult">
        SELECT
        <include refid="Base_Column_List" />
        FROM buyer_requirement
        WHERE id = #{id}
          AND buyer_id = #{buyerId}
    </select>

    <insert id="insertBuyerRequirement" parameterType="com.ruoyi.system.domain.BuyerRequirement" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO buyer_requirement
        (buyer_id, product_name, quantity, specs, status, created_at, updated_at)
        VALUES
        (#{buyerId}, #{productName}, #{quantity}, #{specs}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateStatus">
        UPDATE buyer_requirement
        SET status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
          AND buyer_id = #{buyerId}
    </update>

</mapper>
