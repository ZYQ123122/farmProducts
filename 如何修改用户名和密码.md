# 如何修改农户和买家的用户名和密码

## 当前用户信息

- **管理员**：ID=1，用户名=`admin`，密码=`admin123`
- **农户**：ID=100，用户名=`1234`，密码=`123456`
- **买家**：ID=101，用户名=`buyer1`，密码=`buyer1234`

---

## ⚠️ 重要提示：如果管理员无法登录

如果管理员账号无法登录，请立即执行以下脚本修复：

### 快速修复所有用户密码（推荐）

1. **打开 SQL Server Management Studio (SSMS)**
2. **连接到数据库 `ry`**
3. **执行脚本**：`sql/fix_all_passwords_sqlserver.sql`
   - 此脚本会自动修复管理员、农户和买家的密码
   - 如果用户不存在，会自动创建

执行后即可使用以下账号登录：
- **管理员**：`admin` / `admin123`
- **农户**：`1234` / `123456`
- **买家**：`buyer1` / `buyer1234`

---

## 方法一：通过数据库直接修改（推荐）

### 步骤

1. **打开 SQL Server Management Studio (SSMS)**
   - 连接到你的 SQL Server 数据库

2. **执行 SQL 脚本**
   
   **选项A：修复所有用户（包括管理员）**
   - 打开文件：`sql/fix_all_passwords_sqlserver.sql`
   - 在 SSMS 中执行该脚本
   - 脚本会自动修复管理员、农户和买家的密码
   
   **选项B：仅更新农户和买家**
   - 打开文件：`sql/update_user_credentials_sqlserver.sql`
   - 在 SSMS 中执行该脚本
   - 脚本会自动更新管理员、农户和买家的用户名和密码

3. **自定义修改**
   如果需要修改为其他用户名和密码，可以编辑脚本中的以下部分：

```sql
-- 修改农户（ID=100）
UPDATE sys_user 
SET 
    username = '新用户名',
    password_hash = LOWER(CONVERT(VARCHAR(32), HASHBYTES('MD5', '新用户名' + '新密码'), 2)),
    updated_at = GETDATE()
WHERE id = 100;
GO

-- 修改买家（ID=101）
UPDATE sys_user 
SET 
    username = '新用户名',
    password_hash = LOWER(CONVERT(VARCHAR(32), HASHBYTES('MD5', '新用户名' + '新密码'), 2)),
    updated_at = GETDATE()
WHERE id = 101;
GO
```

**重要提示**：
- 密码加密方式：`MD5(username + password)`
- 例如：用户名=`1234`，密码=`123456`，则加密值为 `MD5('1234' + '123456')` = `MD5('1234123456')`

---

## 方法二：通过系统界面修改（需要管理员权限）

### 修改用户名

1. **登录系统**
   - 使用管理员账号登录：`http://127.0.0.1:80/`
   - 用户名：`admin`，密码：`admin123`

2. **进入用户管理**
   - 点击左侧菜单：**系统管理** → **用户管理**
   - 找到要修改的用户（农户 ID=100 或买家 ID=101）

3. **编辑用户**
   - 点击用户行的 **修改** 按钮
   - 修改 **登录账号**（即用户名）
   - 点击 **提交** 保存

### 修改密码

有两种方式：

#### 方式A：管理员重置用户密码

1. **进入用户管理**
   - 系统管理 → 用户管理
   - 找到要修改的用户

2. **重置密码**
   - 点击用户行的 **重置密码** 按钮
   - 输入新密码
   - 点击 **提交** 保存

#### 方式B：用户自己修改密码（登录后）

1. **登录系统**
   - 使用农户或买家账号登录

2. **进入个人中心**
   - 点击右上角头像 → **个人中心**

3. **修改密码**
   - 点击 **修改密码** 标签
   - 输入旧密码和新密码
   - 点击 **提交** 保存

---

## 验证修改结果

### 方法1：在数据库中查询

```sql
SELECT 
    id,
    username,
    password_hash,
    role,
    name,
    is_verified
FROM sys_user
WHERE id IN (100, 101);
```

### 方法2：尝试登录

- 使用修改后的用户名和密码登录系统
- 登录地址：`http://127.0.0.1:80/`

---

## 常见问题

### Q1：修改密码后无法登录？

**A**：检查以下几点：
1. 确认密码是否正确（注意大小写）
2. 确认用户名是否正确
3. 如果通过数据库修改，确认使用了正确的 MD5 加密方式
4. 清除浏览器缓存后重试

### Q2：如何计算密码的 MD5 值？

**A**：密码加密公式：`MD5(username + password)`

**示例**：
- 用户名：`1234`，密码：`123456`
- 加密值 = `MD5('1234' + '123456')` = `MD5('1234123456')`

可以使用在线 MD5 工具或 SQL Server 的 `HASHBYTES` 函数计算。

### Q3：忘记管理员密码或管理员无法登录怎么办？

**A**：立即执行修复脚本：

1. **打开 SQL Server Management Studio (SSMS)**
2. **连接到数据库 `ry`**
3. **执行脚本**：`sql/fix_all_passwords_sqlserver.sql`

或者手动执行以下 SQL：

```sql
-- 重置管理员密码为 admin123
USE ry;
GO

UPDATE sys_user 
SET 
    username = 'admin',
    password_hash = LOWER(CONVERT(VARCHAR(32), HASHBYTES('MD5', 'admin' + 'admin123'), 2)),
    role = 'admin',
    name = '管理员',
    email = 'admin@example.com',
    phone = '15888888888',
    is_verified = 1,
    updated_at = GETDATE()
WHERE username = 'admin' OR id = 1;
GO
```

---

## 快速参考

| 用户类型 | ID | 默认用户名 | 默认密码 | 角色 |
|---------|----|-----------|---------|------|
| 管理员 | 1 | admin | admin123 | admin |
| 农户 | 100 | 1234 | 123456 | farmer |
| 买家 | 101 | buyer1 | buyer1234 | buyer |

---

**提示**：建议定期修改密码，确保账户安全！

