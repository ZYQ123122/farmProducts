# 社区交流模块使用说明

## 📋 模块概述

社区交流模块是一个完整的论坛系统，支持主题发布、评论、点赞等功能。

## 🗄️ 数据库表结构

### 1. 论坛分类表 (forum_categories)
- 存储论坛分类信息
- 默认包含：种植技术、市场行情、政策法规、经验交流、问题求助

### 2. 讨论主题表 (forum_topics)
- 存储用户发布的主题
- 支持置顶、分类、浏览统计等功能

### 3. 评论表 (forum_comments)
- 存储主题下的评论
- 支持回复和引用评论

### 4. 附件表 (forum_attachments)
- 存储主题或评论的附件（图片、文档等）

### 5. 点赞表 (forum_likes)
- 存储评论的点赞记录
- 防止重复点赞

## 🚀 部署步骤

### 步骤一：执行数据库脚本

1. 打开 SQL Server Management Studio (SSMS)
2. 连接到数据库服务器（localhost，用户名：admin，密码：admin123）
3. 选择数据库 `ry`
4. 打开并执行脚本：`sql/forum_tables_sqlserver.sql`
5. 确认所有表创建成功

### 步骤二：验证代码

1. 在 IDEA 中刷新 Maven 项目
2. 检查是否有编译错误
3. 如果一切正常，启动应用

### 步骤三：访问功能

1. 启动应用后，使用农户账号登录（如：`1234` / `123456`）
2. 在左侧菜单点击 **"交流社区"**
3. 即可使用社区交流功能

## 📁 文件结构

### 后端代码

```
ruoyi-system/src/main/java/com/ruoyi/system/
├── domain/
│   ├── ForumCategory.java          # 分类实体
│   ├── ForumTopic.java             # 主题实体
│   ├── ForumComment.java           # 评论实体
│   ├── ForumAttachment.java        # 附件实体
│   └── ForumLike.java              # 点赞实体
├── mapper/
│   ├── ForumCategoryMapper.java
│   ├── ForumTopicMapper.java
│   ├── ForumCommentMapper.java
│   ├── ForumAttachmentMapper.java
│   └── ForumLikeMapper.java
└── service/
    ├── IForumCategoryService.java
    ├── IForumTopicService.java
    ├── IForumCommentService.java
    ├── IForumLikeService.java
    └── impl/
        ├── ForumCategoryServiceImpl.java
        ├── ForumTopicServiceImpl.java
        ├── ForumCommentServiceImpl.java
        └── ForumLikeServiceImpl.java

ruoyi-system/src/main/resources/mapper/system/
├── ForumCategoryMapper.xml
├── ForumTopicMapper.xml
├── ForumCommentMapper.xml
├── ForumAttachmentMapper.xml
└── ForumLikeMapper.xml

ruoyi-admin/src/main/java/com/ruoyi/web/controller/system/
└── ForumController.java             # 论坛控制器
```

### 前端页面

```
ruoyi-admin/src/main/resources/templates/farmer/community/
├── index.html      # 主题列表页
├── detail.html     # 主题详情页
└── add.html        # 发布主题页
```

## 🎯 功能说明

### 1. 主题列表页 (`/farmer/forum/index`)

**功能：**
- 显示所有主题列表
- 支持按分类筛选
- 显示主题的浏览数、回复数
- 置顶主题会高亮显示
- 点击主题可进入详情页

**操作：**
- 点击 **"发布主题"** 按钮可发布新主题
- 点击分类下拉框可筛选主题
- 点击主题标题可查看详情

### 2. 主题详情页 (`/farmer/forum/detail`)

**功能：**
- 显示主题的完整内容
- 显示所有评论
- 支持发表评论
- 支持点赞评论
- 支持回复评论
- 支持删除自己的评论

**操作：**
- 在评论框输入内容，点击 **"发表评论"** 发表评论
- 点击评论下方的 **"点赞"** 可点赞/取消点赞
- 点击 **"回复"** 可回复该评论
- 点击 **"删除"** 可删除自己的评论（仅自己可见）

### 3. 发布主题页 (`/farmer/forum/add`)

**功能：**
- 发布新的讨论主题
- 选择分类（可选）
- 输入标题和内容

**操作：**
- 填写主题标题（必填）
- 选择分类（可选）
- 输入主题内容（必填）
- 点击 **"发布"** 提交

## 🔧 API 接口

### 主题相关

- `GET /farmer/forum/index` - 主题列表页
- `GET /farmer/forum/detail?id={id}` - 主题详情页
- `GET /farmer/forum/add` - 发布主题页
- `POST /farmer/forum/save` - 保存主题
- `POST /farmer/forum/delete` - 删除主题

### 评论相关

- `POST /farmer/forum/comment/save` - 保存评论
- `POST /farmer/forum/comment/like` - 点赞/取消点赞
- `POST /farmer/forum/comment/delete` - 删除评论

## ⚠️ 注意事项

1. **权限控制**
   - 只有登录用户才能发布主题和评论
   - 只能删除自己发布的内容

2. **数据安全**
   - 所有用户输入都经过后端验证
   - 防止 SQL 注入和 XSS 攻击

3. **性能优化**
   - 主题列表支持分页（可扩展）
   - 评论数量通过触发器自动更新

4. **扩展功能**
   - 附件上传功能已预留接口，可后续扩展
   - 支持图片上传、文件下载等功能

## 🐛 常见问题

### Q1: 执行数据库脚本时报错

**解决方案：**
- 确保 SQL Server 服务正在运行
- 确保数据库 `ry` 已创建
- 确保用户 `admin` 有足够权限

### Q2: 页面显示 404 错误

**解决方案：**
- 检查 Controller 路径是否正确
- 检查前端页面路径是否正确
- 确保已登录且角色为 `farmer`

### Q3: 无法发布主题

**解决方案：**
- 检查是否已登录
- 检查标题和内容是否填写
- 查看浏览器控制台是否有错误信息

### Q4: 评论无法显示

**解决方案：**
- 检查数据库表 `forum_comments` 是否有数据
- 检查 Mapper XML 文件是否正确
- 查看应用日志是否有错误

## 📝 后续扩展建议

1. **分页功能**
   - 主题列表和评论列表支持分页

2. **搜索功能**
   - 支持按标题、内容搜索主题

3. **附件上传**
   - 支持图片、文档上传
   - 支持图片预览

4. **富文本编辑器**
   - 使用富文本编辑器编辑主题和评论

5. **通知功能**
   - 评论被回复时通知用户
   - 主题被评论时通知作者

6. **管理功能**
   - 管理员可置顶/取消置顶主题
   - 管理员可删除不当内容

---

**提示**：如有问题，请查看应用日志或联系开发人员。

