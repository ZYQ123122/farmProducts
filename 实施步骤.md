# 银行贷款系统实施步骤

## 第一步：执行数据库脚本

1. 连接MySQL数据库
2. 执行以下SQL脚本（按顺序）：

```bash
# 1. 创建银行相关表
mysql -u用户名 -p数据库名 < farmProducts/sql/init_bank_tables.sql

# 2. 如果finance_application表已存在，需要执行ALTER语句
# 可以在MySQL客户端中执行以下SQL：
```

```sql
-- 检查表结构
DESC finance_application;

-- 如果product_id字段不存在，执行：
ALTER TABLE finance_application ADD COLUMN product_id bigint(20) COMMENT '选择的贷款产品ID' AFTER farmer_id;

-- 如果reviewer_id字段不存在，执行：
ALTER TABLE finance_application ADD COLUMN reviewer_id bigint(20) COMMENT '审批银行用户ID' AFTER status;

-- 修改bank_id字段注释
ALTER TABLE finance_application MODIFY COLUMN bank_id bigint(20) COMMENT '银行ID（产品所属银行）';

-- 添加索引
ALTER TABLE finance_application ADD KEY idx_finance_product (product_id);
```

## 第二步：创建银行账号

1. **在系统用户管理中创建银行用户**
   - 登录系统管理后台
   - 进入"系统管理" → "用户管理"
   - 创建新用户，设置登录名和密码
   - 记录用户ID（user_id）

2. **在bank_info表中插入银行信息**

```sql
INSERT INTO bank_info (
    bank_code, 
    bank_name, 
    user_id, 
    contact_person, 
    contact_phone, 
    contact_email, 
    address, 
    status
) VALUES (
    'BANK001',                    -- 银行代码（唯一）
    '中国农业银行',               -- 银行名称
    用户ID,                       -- 替换为实际用户ID
    '张经理',                     -- 联系人
    '13800138000',               -- 联系电话
    'bank@example.com',          -- 联系邮箱
    '北京市朝阳区xxx路xxx号',    -- 银行地址
    '0'                          -- 状态：0正常，1停用
);
```

## 第三步：重新编译项目

```bash
cd farmProducts
mvn clean install -DskipTests
```

## 第四步：重启项目

```bash
# Windows
cd farmProducts/ruoyi-admin/target
java -Xms256m -Xmx1024m -XX:MetaspaceSize=128m -XX:MaxMetaspaceSize=512m -jar ruoyi-admin.jar

# 或者使用批处理文件
cd farmProducts/bin
run.bat
```

## 第五步：测试功能

### 1. 银行端测试

1. **登录银行账号**
   - 使用创建的银行用户账号登录
   - 进入银行工作台

2. **创建贷款产品**
   - 点击"产品管理" → "贷款产品管理"
   - 点击"新增产品"
   - 填写产品信息：
     - 产品名称：如"农户小额贷款"
     - 最小金额：10000
     - 最大金额：500000
     - 最短期限：3个月
     - 最长期限：36个月
     - 年利率：5.5%
     - 产品描述和申请条件
   - 状态选择"上架"
   - 保存

3. **查看融资申请**
   - 点击"融资申请列表"
   - 查看农户提交的申请

### 2. 农户端测试

1. **登录农户账号**
   - 使用农户账号登录

2. **查看贷款产品**
   - 进入"融资管理" → "融资申请"
   - 页面会自动加载所有上架的贷款产品
   - 选择产品后，会显示产品详细信息

3. **提交融资申请**
   - 选择贷款产品
   - 填写申请金额（需在产品范围内）
   - 填写贷款期限（需在产品范围内）
   - 填写贷款用途和抵押物说明
   - 上传抵押物照片或文件
   - 提交申请

4. **查看通知**
   - 进入"融资管理" → "通知中心"
   - 查看提交通知和审批结果通知

### 3. 银行审批测试

1. **审批申请**
   - 银行登录后，进入"融资申请列表"
   - 点击"审批/查看"
   - 查看申请详情（包括选择的产品、金额、抵押物等）
   - 选择审批结果（通过/拒绝）
   - 填写审批意见
   - 提交审批

2. **验证通知**
   - 农户登录后，进入"通知中心"
   - 应该能看到审批结果通知

## 第六步：验证数据

```sql
-- 查看银行信息
SELECT * FROM bank_info;

-- 查看贷款产品
SELECT * FROM bank_loan_product;

-- 查看融资申请
SELECT * FROM finance_application;

-- 查看通知
SELECT * FROM finance_notification;
```

## 常见问题排查

1. **编译错误**
   - 检查Java版本（需要JDK 1.8+）
   - 检查Maven配置
   - 清理并重新编译：`mvn clean install -DskipTests`

2. **数据库连接错误**
   - 检查`application-druid.yml`中的数据库配置
   - 确认数据库服务已启动

3. **页面404错误**
   - 检查Controller路径是否正确
   - 检查模板文件是否存在

4. **产品不显示**
   - 检查产品状态是否为"上架"（status='1'）
   - 检查银行状态是否为"正常"（status='0'）

5. **通知未发送**
   - 检查通知表是否正确插入数据
   - 检查用户ID是否正确

## 完成检查清单

- [ ] 数据库表创建成功
- [ ] 银行账号创建并关联
- [ ] 项目编译成功
- [ ] 项目启动成功
- [ ] 银行可以创建产品
- [ ] 农户可以看到产品
- [ ] 农户可以提交申请
- [ ] 银行可以审批申请
- [ ] 农户可以收到通知

完成以上步骤后，银行贷款系统即可正常使用！

