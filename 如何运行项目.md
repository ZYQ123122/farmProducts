# 如何运行项目

## ✅ 好消息：不需要手动连接数据库！

Spring Boot 应用在启动时会**自动连接数据库**，你只需要在 IDEA 中直接运行即可。

---

## 🚀 日常运行步骤

### 1. 确保 SQL Server 服务正在运行

**检查 SQL Server 服务状态：**

- 按 `Win + R`，输入 `services.msc`，回车
- 找到 **SQL Server (MSSQLSERVER)** 或 **SQL Server (实例名)**
- 确保状态为 **正在运行**
- 如果未运行，右键点击 → **启动**

或者通过命令行检查：
```powershell
# PowerShell
Get-Service | Where-Object {$_.Name -like "*SQL*"}
```

### 2. 在 IDEA 中运行项目

**方法一：运行主类（推荐）**

1. 打开项目：`ruoyi-admin/src/main/java/com/ruoyi/RuoYiApplication.java`
2. 右键点击 `RuoYiApplication` 类
3. 选择 **Run 'RuoYiApplication'** 或按 `Shift + F10`
4. 等待启动完成（看到 "若依启动成功" 的 ASCII 艺术字）

**方法二：使用 Maven 运行**

1. 打开右侧 **Maven** 工具窗口
2. 展开 `ruoyi-admin` → `Plugins` → `spring-boot`
3. 双击 `spring-boot:run`

**方法三：使用运行配置**

1. 点击 IDEA 顶部工具栏的运行配置下拉框
2. 选择 `RuoYiApplication`
3. 点击绿色运行按钮 ▶️

---

## 📋 启动检查清单

运行前确认：

- ✅ SQL Server 服务正在运行
- ✅ 数据库 `ry` 已创建
- ✅ 数据库用户 `admin` 密码 `admin123` 正确
- ✅ 端口 1433 未被占用（SQL Server 默认端口）
- ✅ 端口 80 未被占用（应用默认端口）

---

## 🔍 启动后验证

### 1. 查看控制台输出

启动成功后，控制台会显示：
```
(♥◠‿◠)ﾉﾞ  若依启动成功   ლ(´ڡ`ლ)ﾞ  
 .-------.       ____     __        
 |  _ _   \      \   \   /  /    
 | ( ' )  |       \  _. /  '       
 |(_ o _) /        _( )_ .'         
 | (_,_).' __  ___(_ o _)'          
 |  |\ \  |  ||   |(_,_)'         
 |  | \ `'   /|   `-'  /           
 |  |  \    /  \      /           
 ''-'   `'-'    `-..-'              
```

### 2. 访问应用

打开浏览器，访问：
- **登录页面**：`http://127.0.0.1:80/` 或 `http://localhost:80/`
- **Druid 监控**：`http://127.0.0.1:80/druid/index.html`（用户名：`ruoyi`，密码：`123456`）

### 3. 测试登录

使用以下账号登录：
- **管理员**：`admin` / `admin123`
- **农户**：`1234` / `123456`
- **买家**：`buyer1` / `buyer1234`

---

## ⚠️ 常见问题

### Q1：启动失败，提示 "无法连接到数据库"

**解决方案：**

1. **检查 SQL Server 服务是否运行**
   ```powershell
   Get-Service | Where-Object {$_.Name -like "*SQL*"}
   ```

2. **检查数据库连接配置**
   - 打开：`ruoyi-admin/src/main/resources/application-druid.yml`
   - 确认以下配置正确：
     ```yaml
     url: jdbc:sqlserver://localhost:1433;databaseName=ry;encrypt=false;trustServerCertificate=true
     username: admin
     password: admin123
     ```

3. **测试数据库连接**
   - 打开 SQL Server Management Studio (SSMS)
   - 尝试连接：服务器名=`localhost`，用户名=`admin`，密码=`admin123`
   - 如果无法连接，检查 SQL Server 是否允许 SQL Server 身份验证

### Q2：启动失败，提示 "端口 80 已被占用"

**解决方案：**

**方法一：修改应用端口**
- 打开：`ruoyi-admin/src/main/resources/application.yml`
- 修改：
  ```yaml
  server:
    port: 8080  # 改为其他端口，如 8080
  ```
- 然后访问：`http://127.0.0.1:8080/`

**方法二：关闭占用端口 80 的程序**
```powershell
# 查看占用端口 80 的进程
netstat -ano | findstr :80

# 结束进程（替换 PID 为实际进程ID）
taskkill /PID <PID> /F
```

### Q3：启动成功但无法登录

**解决方案：**

1. **检查用户是否存在**
   - 打开 SSMS，连接到数据库 `ry`
   - 执行：
     ```sql
     SELECT id, username, password_hash, role 
     FROM sys_user 
     WHERE username IN ('admin', '1234', 'buyer1');
     ```

2. **如果用户不存在或密码错误**
   - 执行修复脚本：`sql/fix_all_passwords_sqlserver.sql`
   - 详见：`如何修改用户名和密码.md`

### Q4：启动很慢或卡住

**可能原因：**
- Maven 正在下载依赖（首次运行）
- 数据库连接超时
- 端口被占用

**解决方案：**
- 等待 Maven 下载完成（查看 IDEA 底部进度条）
- 检查 SQL Server 服务状态
- 检查网络连接

---

## 📝 数据库配置说明

数据库连接配置在：`ruoyi-admin/src/main/resources/application-druid.yml`

```yaml
spring:
  datasource:
    druid:
      master:
        url: jdbc:sqlserver://localhost:1433;databaseName=ry;encrypt=false;trustServerCertificate=true
        username: admin
        password: admin123
```

**重要提示：**
- 应用启动时会**自动读取**这个配置文件
- **不需要**手动连接数据库
- 只需要确保 SQL Server 服务正在运行

---

## 🎯 总结

**日常运行流程：**

1. ✅ 确保 SQL Server 服务运行
2. ✅ 在 IDEA 中运行 `RuoYiApplication`
3. ✅ 等待启动完成
4. ✅ 访问 `http://127.0.0.1:80/`
5. ✅ 使用账号登录

**不需要：**
- ❌ 手动打开 SSMS
- ❌ 手动连接数据库
- ❌ 执行任何 SQL 脚本（除非需要修改数据）

---

## 🔧 开发建议

### 热部署（可选）

如果修改了代码，可以启用热部署：
- 修改代码后，IDEA 会自动重新编译
- Spring Boot DevTools 会自动重启应用
- 无需手动重启

### 调试模式

在 IDEA 中设置断点，然后：
- 右键 `RuoYiApplication` → **Debug 'RuoYiApplication'**
- 或按 `Shift + F9`

---

**提示**：如果遇到问题，先检查 SQL Server 服务是否运行，这是最常见的原因！








