# 生成中文测试报告说明

## 问题说明

Maven Surefire Report Plugin 生成的 HTML 报告默认是英文的，因为它是基于标准模板生成的，无法直接修改为中文。

## 解决方案

我创建了一个 Java 工具 `GenerateChineseReport.java`，它可以：
1. 自动扫描所有模块的 XML 测试报告
2. 解析测试结果数据
3. 生成完全中文化的 HTML 报告

## 使用方法

### 方法一：使用批处理脚本（Windows）

```bash
generate-chinese-report.bat
```

### 方法二：使用 Shell 脚本（Linux/Mac）

```bash
chmod +x generate-chinese-report.sh
./generate-chinese-report.sh
```

### 方法三：手动运行

1. 先运行测试生成 XML 报告：
   ```bash
   mvn test
   ```

2. 编译 Java 工具：
   ```bash
   javac -encoding UTF-8 GenerateChineseReport.java
   ```

3. 运行工具生成中文报告：
   ```bash
   java GenerateChineseReport . test-report-zh.html
   ```

## 生成的文件

- **test-report-zh.html** - 中文版测试报告（在项目根目录）

## 报告内容

中文报告包含：
- ✅ 测试摘要（总数、错误、失败、跳过、成功率、耗时）
- ✅ 测试套件列表（按测试类分组）
- ✅ 详细测试结果（每个测试用例的状态、耗时、错误信息）
- ✅ 美观的中文界面

## 与英文报告的区别

| 特性 | 英文报告（Surefire） | 中文报告（自定义） |
|------|---------------------|-------------------|
| 语言 | 英文 | 中文 |
| 样式 | Maven 默认样式 | 现代化中文样式 |
| 内容 | 标准格式 | 更详细的中文说明 |
| 生成方式 | Maven 插件自动 | Java 工具生成 |

## 集成到 Maven 构建

如果需要每次测试后自动生成中文报告，可以在 `pom.xml` 中添加：

```xml
<plugin>
    <groupId>org.codehaus.mojo</groupId>
    <artifactId>exec-maven-plugin</artifactId>
    <version>3.1.0</version>
    <executions>
        <execution>
            <phase>test</phase>
            <goals>
                <goal>java</goal>
            </goals>
            <configuration>
                <mainClass>GenerateChineseReport</mainClass>
                <args>
                    <arg>.</arg>
                    <arg>test-report-zh.html</arg>
                </args>
            </configuration>
        </execution>
    </executions>
</plugin>
```

## 注意事项

1. 需要先运行 `mvn test` 生成 XML 报告
2. 确保已安装 JDK 并配置了 JAVA_HOME
3. 生成的 HTML 文件使用 UTF-8 编码，确保浏览器正确显示中文




