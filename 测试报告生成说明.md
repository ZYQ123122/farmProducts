# 单元测试报告生成说明

## 概述

本项目已为农户和买家页面的全部功能编写了完整的JUnit测试用例，并配置了Maven Surefire插件来自动生成单元测试报告。

## 测试用例覆盖范围

### 农户页面功能测试

1. **FarmerControllerTest** - 农户页面路由控制器测试
   - 首页路由测试
   - 主页路由测试
   - 产品信息、产品管理、买家需求、联系买家等页面路由测试
   - 订单管理页面路由测试
   - 专家咨询相关页面路由测试
   - 融资管理相关页面路由测试
   - 社区首页路由测试

2. **FarmerProductApiControllerTest** - 农户产品API测试
   - 获取产品列表（无状态过滤/按状态过滤）
   - 获取产品详情（成功/产品不存在）
   - 添加产品（成功/失败）
   - 更新产品（成功/产品不存在）
   - 删除产品
   - 修改产品状态（成功/状态参数为空）

3. **FarmerOrderApiControllerTest** - 农户订单API测试
   - 获取订单列表
   - 接单功能测试

4. **FarmerBuyerRequirementApiControllerTest** - 农户买家需求API测试
   - 获取买家需求列表

5. **FarmerProductServiceImplTest** - 农户产品服务实现测试
   - 查询产品列表（各种场景）
   - 查询产品详情
   - 新增产品（各种场景）
   - 更新产品
   - 软删除产品
   - 修改产品状态
   - 扣减库存

### 买家页面功能测试

1. **BuyerControllerTest** - 买家页面路由控制器测试
   - 首页路由测试
   - 主页路由测试
   - 需求页面路由测试
   - 交易相关页面路由测试
   - 售后相关页面路由测试

2. **BuyerTradeApiControllerTest** - 买家交易API测试
   - 获取商品列表
   - 获取商品详情（成功/商品不存在）
   - 获取订单列表
   - 创建订单
   - 确认收货

3. **BuyerRequirementApiControllerTest** - 买家需求API测试
   - 获取需求列表
   - 创建需求（成功/失败）
   - 修改需求状态（成功/状态为空/更新失败）

4. **BuyerRequirementServiceImplTest** - 买家需求服务实现测试
   - 根据买家ID查询需求列表
   - 查询所有需求
   - 创建需求
   - 根据ID和买家ID查询需求
   - 更新状态（各种场景）

## 运行测试

### 运行所有测试

在项目根目录执行：

```bash
mvn test
```

### 运行特定模块的测试

#### 运行ruoyi-admin模块测试（农户和买家Controller测试）

```bash
cd ruoyi-admin
mvn test
```

#### 运行ruoyi-system模块测试（服务层测试）

```bash
cd ruoyi-system
mvn test
```

### 运行特定测试类

```bash
mvn test -Dtest=FarmerControllerTest
mvn test -Dtest=BuyerTradeApiControllerTest
```

## 生成测试报告

### 生成XML格式测试报告

运行测试后，XML格式的测试报告会自动生成在以下目录：

- `ruoyi-admin/target/surefire-reports/`
- `ruoyi-system/target/surefire-reports/`

### 生成HTML格式测试报告

执行以下命令生成HTML格式的测试报告：

```bash
# 在项目根目录
mvn surefire-report:report

# 或在特定模块目录
cd ruoyi-admin
mvn surefire-report:report

cd ruoyi-system
mvn surefire-report:report
```

HTML报告将生成在：
- `ruoyi-admin/target/site/surefire-report.html`
- `ruoyi-system/target/site/surefire-report.html`

### 查看测试报告

1. **XML报告**：位于各模块的 `target/surefire-reports/` 目录
   - `TEST-*.xml` - 测试结果XML文件
   - `*.txt` - 测试输出文本文件

2. **HTML报告**：位于各模块的 `target/site/` 目录
   - `surefire-report.html` - 可浏览器打开查看

## 测试报告内容

测试报告包含以下信息：

1. **测试概览**
   - 测试总数
   - 成功数量
   - 失败数量
   - 跳过数量
   - 执行时间

2. **测试详情**
   - 每个测试类的执行结果
   - 每个测试方法的执行结果
   - 失败测试的错误信息
   - 执行时间统计

3. **测试覆盖率**
   - 代码覆盖率统计（如果配置了覆盖率工具）

## 注意事项

1. 确保已安装Java 8或更高版本
2. 确保已安装Maven 3.6或更高版本
3. 运行测试前确保项目已正确编译
4. 如果测试失败，检查测试报告中的错误信息进行修复

## 持续集成

可以将测试集成到CI/CD流程中：

```bash
# 在CI脚本中
mvn clean test surefire-report:report
```

生成的报告可以上传到CI系统的报告展示页面。

